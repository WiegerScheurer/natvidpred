name: meg_encoding
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - numpy>=1.24
  - scipy>=1.11
  - pandas>=2.0
  - scikit-learn>=1.3
  - matplotlib>=3.7
  - h5py>=3.9
  - opencv          # cv2
  - scikit-image    # skimage.metrics.structural_similarity
  - pip
  - pip:
      - mne          # optional: for topomaps (install separately if needed)
